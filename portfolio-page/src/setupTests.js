// jest-dom adds custom matchers for asserting on DOM nodes, for example
// expect(element).toHaveTextContent(/react/i). The /vitest entry registers
// them with Vitest's expect.
import '@testing-library/jest-dom/vitest';
